<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Closest_Future_Event_Account__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    NOT(ISBLANK(Future_Sales_Event__c)) &amp;&amp; NOT(ISBLANK(Future_Sales_Event_Lead__c)),
    IF(
        Future_Sales_Event__c &gt; Future_Sales_Event_Lead__c,
        Future_Sales_Event__c,
        Future_Sales_Event_Lead__c
    ),
    IF(
        NOT(ISBLANK(Future_Sales_Event__c)),
        Future_Sales_Event__c,
        IF(
            NOT(ISBLANK(Future_Sales_Event_Lead__c)),
            Future_Sales_Event_Lead__c,
            NULL
        )
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Future Event - Account</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
