<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Last_Sales_Event_Account__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    NOT(ISBLANK(Last_Sales_Event_Lead__c)) &amp;&amp; NOT(ISBLANK(Last_Sales_Event__c)),
    IF(
        Last_Sales_Event_Lead__c &gt; Last_Sales_Event__c,
        Last_Sales_Event_Lead__c,
        Last_Sales_Event__c
    ),
    IF(
        NOT(ISBLANK(Last_Sales_Event_Lead__c)),
        Last_Sales_Event_Lead__c,
        IF(
            NOT(ISBLANK(Last_Sales_Event__c)),
            Last_Sales_Event__c,
            NULL  
        )
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Last Sales Event - Account</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
