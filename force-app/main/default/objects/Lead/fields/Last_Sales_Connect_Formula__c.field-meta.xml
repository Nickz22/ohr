<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Last_Sales_Connect_Formula__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    ISBLANK(Last_Sales_Connect__c) &amp;&amp; ISBLANK(Last_Sales_Connect_Event__c), 
    NULL, 
    IF(
        ISBLANK(Last_Sales_Connect__c), 
        Last_Sales_Connect_Event__c, 
        IF(
            ISBLANK(Last_Sales_Connect_Event__c), 
            Last_Sales_Connect__c, 
            IF(
                Last_Sales_Connect__c &gt; Last_Sales_Connect_Event__c, 
                Last_Sales_Connect__c, 
                Last_Sales_Connect_Event__c
            )
        )
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Last Sales Connect Formula</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
