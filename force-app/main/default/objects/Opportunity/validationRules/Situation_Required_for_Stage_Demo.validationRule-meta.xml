<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Situation_Required_for_Stage_Demo</fullName>
    <active>false</active>
    <description>Requires users to fill out Situation field when stage changes to Demo</description>
    <errorConditionFormula>AND (
  NOT(ISPICKVAL(Type, &quot;Existing Business&quot;)),
  ISCHANGED( StageName ),
  ISPICKVAL( StageName, &quot;Demo&quot; ),
  ISBLANK( Situation__c )
)</errorConditionFormula>
    <errorDisplayField>Situation__c</errorDisplayField>
    <errorMessage>Situation field is required before changing Stage to Demo</errorMessage>
</ValidationRule>
